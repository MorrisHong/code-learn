<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.grace.mapper.BoardMapper">

    <insert id="insertSelectKey">
        <selectKey keyProperty="bno" order="BEFORE" resultType="long">
            SELECT SEQ_BOARD.nextval FROM dual
        </selectKey>
        INSERT into tbl_board (bno, title,content,writer)
        values(#{bno}, #{title}, #{content}, #{writer})
    </insert>
    <update id="update">
        UPDATE TBL_BOARD TB
        set title      = ${title},
            CONTENT    = ${content},
            WRITER     = ${writer},
            UPDATEDATE = sysdate
        where bno = #{bno}
    </update>

    <delete id="delete">
        DELETE TBL_BOARD TB
        where bno = #{bno}
    </delete>


    <select id="getList" resultType="me.grace.domain.BoardVO">
        <![CDATA[
        SELECT *
        FROM tbl_board
        where bno > 0
        ]]>
    </select>

    <select id="read" resultType="me.grace.domain.BoardVO">
        SELECT *
        FROM TBL_BOARD TB
        where bno = #{bno}
    </select>
</mapper>